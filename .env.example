# Vite will expose these to the frontend if prefixed with VITE_
# Copy to .env.local and fill with your secrets. Do not commit .env.local.

VITE_SCM_BASE_URL=http://172.31.200.215:8080
# API path for repository list (proxied in dev)
VITE_SCM_REPOS_PATH=/scm/api/v2/repositories
# Prefer a token when available
VITE_SCM_TOKEN=
# Or use basic auth (not both)
VITE_SCM_USERNAME=
VITE_SCM_PASSWORD=
